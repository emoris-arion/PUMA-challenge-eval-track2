# ── PUMA Track 2 submission — Python dependencies ─────────────────────────────
# Base image: pytorch/pytorch:2.4.0-cuda12.4-cudnn9-runtime
# torch + torchvision are already installed in the base image.
# ──────────────────────────────────────────────────────────────────────────────

# Training framework (needed to load Lightning checkpoints)
lightning>=2.1.0

# Data
numpy>=1.24.0
Pillow>=10.0.0
PyYAML>=6.0

# Medical image I/O (reads .mha and .tif input images from Grand Challenge)
SimpleITK>=2.2.0

# Nucleus detection (morphological pipeline)
scipy>=1.10.0

# Metrics (required by BaseModel; scikit-learn used internally)
scikit-learn>=1.3.0

# WandB is imported by BaseModel but not used at inference — keep it lightweight
wandb>=0.16.0
matplotlib>=3.7.0
